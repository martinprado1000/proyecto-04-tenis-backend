version: '3'

services:
  proyecto-04-tenis:
    depends_on:
      - db
    build: 
      context: .
      dockerfile: Dockerfile
    image: proyecto-04-tenis
    container_name: proyecto-04-tenis
    restart: always # reiniciar el contenedor si se detiene
    ports:
      - "${PORT}:${PORT}"
    # working_dir: /var/www/pokedex
    environment:
      #MONGODB: ${MONGODB}
      DATABASE_URI: ${DATABASE_URI}
      PORT: ${PORT}
      #DEFAULT_LIMIT: ${DEFAULT_LIMIT}
      PAGINATIOS_DEFAULT_LIMIT: ${PAGINATIOS_DEFAULT_LIMIT}
    # volumes:
    #   - ./:/var/www/pokedex

  db: 
    image: mongo:5.0.0
    container_name: db_mongo_prod
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGODB_DATABASE: db_mongo_prod
    volumes:
      - ./mongo:/data/db